# Gruber Darker — Glamour JSON (matches Zed theme). Glow does not expand ~ or $HOME in style paths.
style: "{{ .chezmoi.homeDir }}/.config/glow/gruber-darker.json"
mouse: true
pager: true
tui: false
width: 80
all: false
