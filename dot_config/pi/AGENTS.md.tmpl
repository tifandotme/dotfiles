{{ includeTemplate "dot_agents/AGENTS.shared.md.tmpl" . -}}
- For long-running processes, watchers, dev servers, log tails, background shells, or sibling agents, load the `herdr` skill and run them in a Herdr pane or tab. Do not use detached background jobs for these tasks. Herdr panes run Nushell, so use Nushell syntax.
