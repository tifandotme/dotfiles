{{ includeTemplate "dot_agents/AGENTS.shared.md.tmpl" . -}}
- Explanations: use concise ASCII diagrams when they make layouts, flows, or relationships easier to understand.
- For watchers, dev servers, background shells, load the `herdr` skill and run them in a Herdr pane or tab. Do not use detached background jobs for these tasks. Herdr panes run Nushell, so use Nushell syntax.
